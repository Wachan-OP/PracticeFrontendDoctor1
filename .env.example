# Backend API URL
VITE_API_URL=http://localhost:5000/api

# ── Set to "true" to bypass backend entirely for UI testing ──────────────────
# Login with:  email: demo@medfit.com  password: Demo@1234
# All API calls return mock data — no backend needed
VITE_MOCK_AUTH=false
